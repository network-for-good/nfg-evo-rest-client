RSpec.describe Nfg::Evo::Rest::Client do
  it "has a version number" do
    expect(Nfg::Evo::Rest::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
